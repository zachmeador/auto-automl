# Run Artifacts

Each AutoML iteration writes artifacts to `experiments/runs/<run_id>/`.

Run outputs are ignored by git by default. Keep reusable templates in `experiments/templates/` and compact accepted/rejected summaries in a registry or memory file when needed.

